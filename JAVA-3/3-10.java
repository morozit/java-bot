public class ChatBot {
    public static int validateMonthCount(int monthCount) {
       //Пишите код под этой строкой
      
       if (monthCount == 0) {
            return 1;
        } else {
            return monthCount;
        }
    }
}